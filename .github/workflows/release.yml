name: Build and Release

on:
  release:
    types: [created]
  push:
    tags:
      - 'v*'
  # Allow manual trigger for testing
  workflow_dispatch:
    inputs:
      upload_to_pypi:
        description: 'Upload to PyPI (only for testing)'
        required: false
        default: false
        type: boolean

permissions:
  contents: write
  id-token: write  # For PyPI trusted publishing

jobs:
  test:
    name: Run tests before release
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v5
      
    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: '3.12'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e .[dev]
        
    - name: Run tests
      run: pytest tests/ -v
      
    - name: Lint code
      run: |
        pip install flake8
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

  build:
    name: Build distribution packages
    needs: test
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v5
      
    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: '3.12'
        
    - name: Install build dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build twine
        
    - name: Build packages
      run: python -m build
      
    - name: Verify packages
      run: |
        twine check dist/*
        python -m pip install dist/*.whl
        python -c "import pyepisuite; print(f'Successfully installed PyEPISuite {pyepisuite.__version__}')"
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v7
      with:
        name: dist-packages
        path: dist/
        retention-days: 30

  publish-to-pypi:
    name: Publish to PyPI
    needs: [test, build]
    runs-on: ubuntu-latest
    # Publish on a v* tag push, on release creation, or on an explicit manual run.
    #
    # The tag-push arm is what actually fires: `release: created` does not trigger
    # workflows when the release is made by create-release.yml, because events
    # raised with the default GITHUB_TOKEN never start new runs.
    #
    # `inputs.upload_to_pypi` is used bare, not compared to 'true'. It is declared
    # `type: boolean`, and GitHub's loose `==` casts mismatched operands to numbers,
    # so `true == 'true'` evaluates as `1 == NaN` -> always false.
    if: >-
      github.event_name == 'release'
      || (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v'))
      || (github.event_name == 'workflow_dispatch' && inputs.upload_to_pypi)
      
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v8
      with:
        name: dist-packages
        path: dist/
        
    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        packages-dir: dist/
        verify-metadata: true
        verbose: true
        print-hash: true

  publish-to-github:
    name: Upload to GitHub Release
    needs: [test, build]
    runs-on: ubuntu-latest
    # Only run for releases or tags
    if: github.event_name == 'release' || startsWith(github.ref, 'refs/tags/')
    
    steps:
    - name: Download build artifacts
      uses: actions/download-artifact@v8
      with:
        name: dist-packages
        path: dist/
        
    - name: Upload to GitHub Release
      uses: softprops/action-gh-release@v3
      with:
        files: |
          dist/*.whl
          dist/*.tar.gz
        tag_name: ${{ github.ref_name }}
        generate_release_notes: true
        draft: false
        prerelease: false
